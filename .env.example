# Folder with test files (defaults to ./samples shipped with the repo)
# SAMPLES_DIR=/path/to/your/media
# Where examples write their results
OUTPUT_DIR=./output
# Override binaries if they are not on PATH
# FFMPEG_BIN=/opt/homebrew/bin/ffmpeg
# FFPROBE_BIN=/opt/homebrew/bin/ffprobe
